{
  "name": "sentinela-projeto-eden",
  "version": "1.0.0",
  "homepage": "https://gabrielfvm.github.io/Sentinela_projeto_EDEN",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "cd dist && git init && git checkout -b gh-pages && git add -A && git commit -m \"Deploy\" && git remote add origin https://github.com/GabrielFVM/Sentinela_projeto_EDEN.git && git push -f origin gh-pages && cd .. && rmdir /s /q dist\\.git"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.10.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
